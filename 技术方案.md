# Merge-2 二合游戏技术方案

> **文档版本**：v1.0
> **更新日期**：2026-06-08
> **基于需求文档**：二合游戏 PRD v1.0
> **目标受众**：研发团队（前端/后端/测试）

---

## 目录

1. [技术选型与总体架构](#1-技术选型与总体架构)
2. [P0 阶段：核心玩法原型（前端可玩）](#2-p0-阶段核心玩法原型前端可玩)
3. [P1 阶段：后端接入与数据持久化](#3-p1-阶段后端接入与数据持久化)
4. [P2 阶段：客户端/美术/商业化](#4-p2-阶段客户端美术商业化)
5. [数据模型设计](#5-数据模型设计)
6. [API 设计](#6-api-设计)
7. [部署方案](#7-部署方案)

---

## 1. 技术选型与总体架构

### 1.1 技术选型

| 层级 | 选型 | 说明 |
|------|------|------|
| **前端引擎** | 纯 HTML/CSS/JS（P0）→ Unity（P2） | P0 快速验证，P2 转原生引擎 |
| **后端语言** | Go（Gin 框架） | 高并发、低延迟，团队熟悉 |
| **数据库** | SQLite（P0）→ MySQL + Redis（P1+） | P0 零依赖，P1 上持久化 |
| **实时通信** | WebSocket（P1+） | 排行榜、活动同步 |
| **部署** | Docker + 腾讯云 CVM | 一键部署 |

### 1.2 总体架构（P0 → P1 → P2 演进）

```
P0（纯前端可玩）:
┌─────────────────────────────────────┐
│          index.html                  │
│  ┌─────────┐  ┌─────────┐          │
│  │ 棋盘渲染 │  │ 合成逻辑 │          │
│  ├─────────┤  ├─────────┤          │
│  │ 生成器   │  │ 订单系统 │          │
│  ├─────────┤  ├─────────┤          │
│  │ 本地存档 │  │ 经济系统 │          │
│  └─────────┘  └─────────┘          │
│     localStorage 持久化              │
└─────────────────────────────────────┘

P1（后端接入）:
┌──────────┐     ┌──────────┐     ┌──────────┐
│  前端 SPA │────▶│ Go API   │────▶│  MySQL   │
│  (Vue/React)│   │ (Gin)    │     │  + Redis │
└──────────┘     └──────────┘     └──────────┘

P2（Unity 原生）:
┌──────────┐     ┌──────────┐     ┌──────────┐
│ Unity    │────▶│ Go API   │────▶│  MySQL   │
│ 客户端    │     │ (Gin)    │     │  + Redis │
└──────────┘     └──────────┘     └──────────┘
```

---

## 2. P0 阶段：核心玩法原型（前端可玩）

### 2.1 目标

**一句话**：一个可玩的 Merge-2 游戏原型，纯前端，用 emoji 表示物品，localStorage 存档。

**验收标准**：
- [x] 6×7 棋盘，可拖拽合成
- [x] 2 种生成器（菜篮、咖啡壶），点击消耗能量产出 emoji 物品
- [x] 3 条物品链（面包链、咖啡链、花链），每链 5 级
- [x] 主线订单系统，完成订单获得金币和经验
- [x] 能量系统（自然恢复 + 购买）
- [x] 建筑升级（面包店、咖啡馆）
- [x] 本地存档（localStorage）
- [x] 基础 UI：棋盘、订单面板、状态栏、生成器栏

### 2.2 物品链设计（emoji 版）

#### 面包链（菜篮产出）

| 等级 | 物品 | Emoji | 说明 |
|------|------|-------|------|
| Lv.1 | 小麦 | 🌾 | 生成器直接产出 |
| Lv.2 | 面粉 | 🥣 | 2×🌾 合成 |
| Lv.3 | 面团 | 🫓 | 2×🥣 合成 |
| Lv.4 | 面包 | 🍞 | 2×🫓 合成 |
| Lv.5 | 蛋糕 | 🎂 | 2×🍞 合成（顶级） |

#### 咖啡链（咖啡壶产出）

| 等级 | 物品 | Emoji | 说明 |
|------|------|-------|------|
| Lv.1 | 咖啡豆 | 🫘 | 生成器直接产出 |
| Lv.2 | 咖啡粉 | 🟤 | 2×🫘 合成 |
| Lv.3 | 黑咖啡 | ☕ | 2×🟤 合成 |
| Lv.4 | 拿铁 | 🥛☕ | 2×☕ 合成 |
| Lv.5 | 特调咖啡 | 🧋 | 2×🥛☕ 合成（顶级） |

#### 花链（花丛产出，自动生成器）

| 等级 | 物品 | Emoji | 说明 |
|------|------|-------|------|
| Lv.1 | 种子 | 🌱 | 自动生成器产出 |
| Lv.2 | 幼苗 | 🌿 | 2×🌱 合成 |
| Lv.3 | 花朵 | 🌸 | 2×🌿 合成 |
| Lv.4 | 花束 | 💐 | 2×🌸 合成 |
| Lv.5 | 花园 | 🌺 | 2×💐 合成（顶级） |

### 2.3 核心模块设计

#### 2.3.1 棋盘模块（Grid.js）

```javascript
// 核心数据结构
class Grid {
  constructor(rows = 6, cols = 7) {
    this.rows = rows;
    this.cols = cols;
    this.cells = new Array(rows * cols).fill(null);
    // cells[i] = { type: 'bread', level: 1, id: 'uuid' } | null
  }

  // 获取物品
  getItem(row, col) { return this.cells[row * this.cols + col]; }

  // 放置物品
  setItem(row, col, item) { this.cells[row * this.cols + col] = item; }

  // 移动物品（拖拽）
  moveItem(fromRow, fromCol, toRow, toCol) { /* 交换或合并 */ }

  // 检查是否可以合成
  canMerge(item1, item2) {
    return item1 && item2 &&
      item1.type === item2.type &&
      item1.level === item2.level &&
      item1.level < 5; // 不超过最高级
  }

  // 执行合成
  merge(row1, col1, row2, col2) {
    const item = this.getItem(row1, col1);
    const newItem = { type: item.type, level: item.level + 1, id: generateId() };
    this.setItem(row1, col1, newItem);
    this.setItem(row2, col2, null);
    // 触发连锁合成检测
    this.checkChainMerge(row1, col1);
    return newItem;
  }

  // 连锁合成（BFS）
  checkChainMerge(row, col) {
    const item = this.getItem(row, col);
    if (!item) return;
    const neighbors = this.getNeighbors(row, col);
    for (const [nr, nc] of neighbors) {
      const neighbor = this.getItem(nr, nc);
      if (this.canMerge(item, neighbor)) {
        this.merge(row, col, nr, nc);
        return; // 递归由外部调用
      }
    }
  }

  // 获取相邻格子
  getNeighbors(row, col) {
    const dirs = [[-1,0],[1,0],[0,-1],[0,1]];
    return dirs
      .map(([dr, dc]) => [row + dr, col + dc])
      .filter(([r, c]) => r >= 0 && r < this.rows && c >= 0 && c < this.cols);
  }
}
```

#### 2.3.2 生成器模块（Generator.js）

```javascript
class Generator {
  constructor(config) {
    this.id = config.id;         // 'basket' | 'coffee_pot' | 'flower_bush'
    this.name = config.name;     // '菜篮' | '咖啡壶' | '花丛'
    this.emoji = config.emoji;   // '🧺' | '☕' | '🌸'
    this.type = config.type;     // 'normal' | 'auto'
    this.level = 1;
    this.maxLevel = 5;
    this.energyCost = config.energyCost || 1;
    this.produceTable = config.produceTable;
    // 自动生成器属性
    this.autoInterval = config.autoInterval || 0; // ms
    this.lastAutoProduce = 0;
    this.cooldownEnd = 0;
  }

  // 点击产出
  produce() {
    if (this.type === 'normal') {
      if (GameState.energy < this.energyCost) return null;
      GameState.energy -= this.energyCost;
    }
    return this.rollItem();
  }

  // 概率产出
  rollItem() {
    const roll = Math.random();
    let cumulative = 0;
    for (const entry of this.produceTable) {
      cumulative += entry.probability;
      if (roll <= cumulative) {
        return { type: entry.type, level: 1 };
      }
    }
    return { type: this.produceTable[0].type, level: 1 };
  }

  // 升级
  upgrade() {
    if (this.level >= this.maxLevel) return false;
    const cost = this.getUpgradeCost();
    if (GameState.coins < cost) return false;
    GameState.coins -= cost;
    this.level++;
    // 升级效果：降低能量消耗、提高高级物品概率
    this.energyCost = Math.max(1, this.energyCost - 1);
    return true;
  }
}

// 生成器配置
const GENERATORS = {
  basket: {
    id: 'basket', name: '菜篮', emoji: '🧺',
    type: 'normal', energyCost: 1,
    produceTable: [
      { type: 'bread', probability: 0.9 },  // 90% 小麦
      { type: 'coffee', probability: 0.1 }, // 10% 咖啡豆
    ]
  },
  coffee_pot: {
    id: 'coffee_pot', name: '咖啡壶', emoji: '☕',
    type: 'normal', energyCost: 1,
    produceTable: [
      { type: 'coffee', probability: 0.9 },
      { type: 'flower', probability: 0.1 },
    ]
  },
  flower_bush: {
    id: 'flower_bush', name: '花丛', emoji: '🌸',
    type: 'auto', autoInterval: 30000, // 30秒自动产出
    produceTable: [
      { type: 'flower', probability: 1.0 },
    ]
  }
};
```

#### 2.3.3 订单模块（Order.js）

```javascript
class OrderSystem {
  constructor() {
    this.mainOrder = null;     // 当前主线订单
    this.sideOrders = [];      // 支线订单（最多3个）
    this.orderQueue = [];      // 订单队列
  }

  // 生成订单
  generateOrder(type, level) {
    const templates = ORDER_TEMPLATES[type];
    const template = templates[Math.floor(Math.random() * templates.length)];
    return {
      id: generateId(),
      type: type,           // 'main' | 'side'
      title: template.title,
      emoji: template.emoji,
      requires: template.requires.map(r => ({
        type: r.type,
        level: r.level,
        count: r.count,
        current: 0
      })),
      rewards: template.rewards,
      completed: false
    };
  }

  // 提交物品到订单
  submitItem(orderId, row, col) {
    const order = this.findOrder(orderId);
    const item = grid.getItem(row, col);
    if (!order || !item) return false;

    const req = order.requires.find(r =>
      r.type === item.type && r.level === item.level && r.current < r.count
    );
    if (!req) return false;

    req.current++;
    grid.setItem(row, col, null);

    // 检查订单是否完成
    if (order.requires.every(r => r.current >= r.count)) {
      order.completed = true;
      this.rewardOrder(order);
    }
    return true;
  }

  // 发放奖励
  rewardOrder(order) {
    GameState.coins += order.rewards.coins;
    GameState.exp += order.rewards.exp;
    if (order.rewards.diamonds) {
      GameState.diamonds += order.rewards.diamonds;
    }
  }
}

// 订单模板
const ORDER_TEMPLATES = {
  main: [
    {
      title: '制作面包',
      emoji: '🍞',
      requires: [{ type: 'bread', level: 4, count: 1 }], // 需要1个面包
      rewards: { coins: 50, exp: 20 }
    },
    {
      title: '冲泡咖啡',
      emoji: '☕',
      requires: [{ type: 'coffee', level: 3, count: 2 }], // 需要2杯黑咖啡
      rewards: { coins: 80, exp: 30 }
    },
    {
      title: '装饰花束',
      emoji: '💐',
      requires: [{ type: 'flower', level: 4, count: 1 }],
      rewards: { coins: 60, exp: 25 }
    }
  ],
  side: [
    {
      title: '收集小麦',
      emoji: '🌾',
      requires: [{ type: 'bread', level: 1, count: 5 }],
      rewards: { coins: 30, exp: 10 }
    }
  ]
};
```

#### 2.3.4 经济与状态管理（GameState.js）

```javascript
class GameState {
  constructor() {
    this.level = 1;           // 玩家等级
    this.exp = 0;             // 当前经验
    this.expToNext = 100;     // 升级所需经验
    this.coins = 0;           // 金币
    this.diamonds = 0;        // 钻石
    this.energy = 100;        // 当前能量
    this.maxEnergy = 100;     // 能量上限
    this.buildings = {};      // 已解锁建筑
    this.generators = {};     // 已解锁生成器
  }

  // 升级
  addExp(amount) {
    this.exp += amount;
    while (this.exp >= this.expToNext) {
      this.exp -= this.expToNext;
      this.level++;
      this.expToNext = Math.floor(this.expToNext * 1.2);
      this.onLevelUp();
    }
  }

  // 能量恢复（每5秒恢复1点）
  tickEnergy() {
    if (this.energy < this.maxEnergy) {
      this.energy = Math.min(this.maxEnergy, this.energy + 1);
    }
  }

  // 保存到 localStorage
  save() {
    const data = {
      grid: grid.cells,
      state: {
        level: this.level, exp: this.exp, expToNext: this.expToNext,
        coins: this.coins, diamonds: this.diamonds,
        energy: this.energy, maxEnergy: this.maxEnergy,
        buildings: this.buildings, generators: this.generators
      },
      orders: { mainOrder: orderSystem.mainOrder, sideOrders: orderSystem.sideOrders }
    };
    localStorage.setItem('merge2_save', JSON.stringify(data));
  }

  // 从 localStorage 读取
  load() {
    const raw = localStorage.getItem('merge2_save');
    if (!raw) return false;
    const data = JSON.parse(raw);
    // 恢复状态...
    return true;
  }
}
```

### 2.4 UI 设计（P0 版）

#### 2.4.1 布局结构

```
┌─────────────────────────────────────────┐
│  🏠 Lv.1  ████████░░  🪙 100  💎 5  ⚡50│  ← 顶部状态栏
├─────────────────────────────────────────┤
│  📋 主线订单                              │
│  🍞 制作面包 (0/1)        [棋盘区域]     │
│  📋 支线 ① 🌾 收集小麦(2/5)              │
│  📋 支线 ② ☕ 黑咖啡(0/2)    6×7 格      │
│                                         │
├─────────────────────────────────────────┤
│  🧺菜篮  ☕咖啡壶  🌸花丛  📦仓库  🏪商店│  ← 底部功能栏
└─────────────────────────────────────────┘
```

#### 2.4.2 交互流程

```
1. 点击生成器 → 消耗能量 → 棋盘空位出现 emoji 物品
2. 拖拽物品到相邻同类型同等级物品 → 合成 → 闪光动画 → 高1级物品
3. 点击棋盘上的物品 → 弹出操作菜单（提交到订单/移动到仓库）
4. 点击订单中的物品槽 → 如果棋盘上有匹配物品，自动提交
5. 点击建筑 → 弹出升级面板（消耗金币）
6. 点击商店 → 购买能量/钻石
```

#### 2.4.3 动画效果（CSS）

```css
/* 合成闪光动画 */
@keyframes merge-flash {
  0% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.3); opacity: 0.8; filter: brightness(1.5); }
  100% { transform: scale(1); opacity: 1; }
}

/* 新物品出现动画 */
@keyframes item-appear {
  0% { transform: scale(0); opacity: 0; }
  60% { transform: scale(1.2); }
  100% { transform: scale(1); opacity: 1; }
}

/* 能量不足抖动 */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-5px); }
  75% { transform: translateX(5px); }
}
```

### 2.5 P0 文件结构

```
merge2-game/
├── index.html          # 入口页面
├── css/
│   ├── style.css       # 全局样式
│   ├── grid.css        # 棋盘样式
│   └── animations.css  # 动画
├── js/
│   ├── main.js         # 入口，初始化
│   ├── Grid.js         # 棋盘逻辑
│   ├── Generator.js    # 生成器逻辑
│   ├── Order.js        # 订单系统
│   ├── GameState.js    # 状态管理
│   ├── UI.js           # UI 渲染
│   └── SaveManager.js  # 存档管理
└── README.md
```

### 2.6 P0 开发计划

| 任务 | 预估工时 | 说明 |
|------|---------|------|
| 棋盘渲染 + 拖拽 | 4h | 6×7 网格，拖拽交换/合成 |
| 合成逻辑 | 3h | 基础合成 + 连锁合成 |
| 生成器系统 | 3h | 点击产出 + 自动产出 |
| 订单系统 | 3h | 主线/支线订单，提交检测 |
| 经济系统 | 2h | 金币/钻石/能量/经验 |
| 建筑升级 | 2h | 面包店/咖啡馆升级 |
| 存档系统 | 1h | localStorage 读写 |
| UI 界面 | 4h | 状态栏/棋盘/订单/生成器 |
| 动画效果 | 2h | 合成闪光/出现/抖动 |
| 测试调优 | 2h | 数值平衡/Bug 修复 |
| **合计** | **26h** | 约 3-4 天 |

---

## 3. P1 阶段：后端接入与数据持久化

### 3.1 后端架构

```
┌─────────────────────────────────────────────┐
│               Nginx (反向代理)                │
├─────────────────────────────────────────────┤
│           Go API Server (Gin)               │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────┐ │
│  │用户服务 │ │游戏服务 │ │订单服务 │ │活动  │ │
│  ├────────┤ ├────────┤ ├────────┤ ├──────┤ │
│  │经济服务 │ │社交服务 │ │管理后台 │ │WS    │ │
│  └────────┘ └────────┘ └────────┘ └──────┘ │
├─────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  MySQL   │  │  Redis   │  │  COS     │  │
│  │ (持久化) │  │ (缓存)   │  │ (资源)   │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
```

### 3.2 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/user/register | 注册 |
| POST | /api/v1/user/login | 登录 |
| GET | /api/v1/user/profile | 获取用户信息 |
| GET | /api/v1/game/state | 获取游戏状态 |
| POST | /api/v1/game/save | 保存游戏状态 |
| POST | /api/v1/game/merge | 执行合成操作 |
| POST | /api/v1/game/produce | 生成器产出 |
| POST | /api/v1/game/submit-order | 提交订单 |
| POST | /api/v1/game/upgrade-building | 升级建筑 |
| POST | /api/v1/game/buy-energy | 购买能量 |
| GET | /api/v1/leaderboard | 排行榜 |

### 3.3 数据表设计

```sql
-- 用户表
CREATE TABLE users (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  open_id VARCHAR(64) NOT NULL UNIQUE,
  nickname VARCHAR(64),
  avatar_url VARCHAR(256),
  level INT DEFAULT 1,
  exp BIGINT DEFAULT 0,
  coins BIGINT DEFAULT 0,
  diamonds INT DEFAULT 0,
  energy INT DEFAULT 100,
  max_energy INT DEFAULT 100,
  last_energy_time BIGINT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 棋盘状态表
CREATE TABLE grid_states (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  row_pos INT NOT NULL,
  col_pos INT NOT NULL,
  item_type VARCHAR(32),
  item_level INT,
  item_id VARCHAR(64),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_pos (user_id, row_pos, col_pos)
);

-- 生成器表
CREATE TABLE generators (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  generator_id VARCHAR(32) NOT NULL,
  level INT DEFAULT 1,
  last_auto_time BIGINT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_gen (user_id, generator_id)
);

-- 建筑表
CREATE TABLE buildings (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  building_id VARCHAR(32) NOT NULL,
  level INT DEFAULT 1,
  unlocked BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_bld (user_id, building_id)
);

-- 订单表
CREATE TABLE orders (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  order_id VARCHAR(64) NOT NULL,
  order_type VARCHAR(16) NOT NULL, -- 'main', 'side', 'daily'
  status VARCHAR(16) DEFAULT 'active', -- 'active', 'completed'
  requires JSON,
  rewards JSON,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  completed_at TIMESTAMP NULL
);
```

---

## 4. P2 阶段：客户端/美术/商业化

### 4.1 Unity 客户端

#### 4.1.1 架构

```
┌─────────────────────────────────────┐
│          UI Layer (UGUI)            │
│  HUD / 棋盘 / 订单 / 商店 / 建筑    │
├─────────────────────────────────────┤
│      Game Logic Layer               │
│  ┌─────────┐ ┌────────┐ ┌───────┐  │
│  │Merge Sys│ │Order   │ │Energy │  │
│  │         │ │System  │ │System │  │
│  ├─────────┤ ├────────┤ ├───────┤  │
│  │Generator│ │Grid    │ │Economy│  │
│  │System   │ │System  │ │System │  │
│  └─────────┘ └────────┘ └───────┘  │
├─────────────────────────────────────┤
│         Data Layer                  │
│  ┌─────────┐ ┌──────────────────┐   │
│  │Local    │ │Network (API + WS)│   │
│  │Storage  │ │                  │   │
│  └─────────┘ └──────────────────┘   │
├─────────────────────────────────────┤
│     Unity Engine (2D Renderer)      │
└─────────────────────────────────────┘
```

#### 4.1.2 核心组件

| 组件 | 说明 |
|------|------|
| GridManager | 棋盘管理，网格渲染，物品放置 |
| MergeController | 合成逻辑，连锁检测，泡泡生成 |
| GeneratorController | 生成器状态机，产出逻辑 |
| OrderController | 订单生成，提交检测，奖励发放 |
| EnergyController | 能量恢复，消耗，购买 |
| BuildingController | 建筑解锁，升级，效果 |
| SaveManager | 本地存档 + 云端同步 |
| UIManager | 全局 UI 管理，面板切换 |
| AnimationController | 合成/升级/获得奖励动画 |
| AudioManager | 音效管理 |
| VFXManager | 粒子特效管理 |

### 4.2 美术资源清单

| 资源类型 | 数量 | 说明 |
|---------|------|------|
| 物品图标 | 15+ | 3 条物品链 × 5 级 |
| 生成器图标 | 4+ | 菜篮/咖啡壶/花丛/特殊 |
| 建筑图标 | 5+ | 面包店/咖啡馆/花店/图书馆/码头 |
| 棋盘背景 | 1 | 小镇风格 |
| UI 元素 | 20+ | 按钮/面板/进度条/弹窗 |
| 角色立绘 | 3+ | 镇长爷爷/安娜/莉莉 |
| 剧情插画 | 10+ | Live2D 或静态 |
| 特效 | 5+ | 合成闪光/升级烟花/获得奖励 |
| 音效 | 15+ | 合成/点击/升级/购买/错误 |

### 4.3 商业化方案

#### 4.3.1 付费点

| 付费点 | 定价 | 触发时机 |
|--------|------|----------|
| 能量包（小） | ¥6 | 能量不足时 |
| 能量包（大） | ¥30 | 活动期间 |
| 钻石直购 | ¥6-¥648 | 商店 |
| 月卡 | ¥25/月 | 每日签到 |
| 通行证 | ¥30/月 | 赛季 |
| 新手礼包 | ¥1 | 首次进入 |
| 限时礼包 | ¥6-¥68 | 活动期间 |

#### 4.3.2 IAA 广告

| 广告类型 | 触发场景 | 奖励 | 每日上限 |
|----------|----------|------|---------|
| 激励视频 | 能量不足 | +20 能量 | 5 次 |
| 激励视频 | 订单等待 | 立即刷新 | 3 次 |
| 激励视频 | 合成加速 | 免费加速 | 3 次 |
| 激励视频 | 双倍奖励 | 订单奖励翻倍 | 10 次 |
| 插屏广告 | 场景切换 | — | 每 3 分钟最多 1 次 |

### 4.4 P2 阶段开发计划

| 任务 | 预估工时 | 说明 |
|------|---------|------|
| Unity 项目初始化 | 8h | 项目搭建，资源导入 |
| 核心逻辑移植 | 16h | 从 JS 移植到 C# |
| 网格渲染 | 8h | 物品渲染，拖拽交互 |
| 动画特效 | 12h | 合成/升级/获得奖励 |
| 音效系统 | 4h | 音效集成 |
| 美术资源制作 | 40h | 全部美术资源 |
| 剧情系统 | 8h | 对话/过场 |
| 活动系统 | 16h | 限时活动 |
| 商业化接入 | 12h | IAP + 广告 SDK |
| 后端联调 | 8h | API 对接 |
| 测试优化 | 16h | 性能/兼容性 |
| **合计** | **~148h** | 约 4-5 周 |

---

## 5. 数据模型设计

### 5.1 核心数据结构

```typescript
// 棋盘物品
interface GridItem {
  id: string;           // 唯一标识
  type: string;         // 'bread' | 'coffee' | 'flower'
  level: number;        // 1-5
  emoji: string;        // '🌾' | '🍞' | ...
}

// 生成器
interface Generator {
  id: string;           // 'basket' | 'coffee_pot' | 'flower_bush'
  name: string;
  emoji: string;
  type: 'normal' | 'auto' | 'special' | 'single_use';
  level: number;
  energyCost: number;
  produceTable: ProduceEntry[];
  cooldownEnd: number;  // 冷却结束时间戳
  remainingUses: number; // 一次性生成器剩余次数
}

// 订单
interface Order {
  id: string;
  type: 'main' | 'side' | 'daily' | 'special';
  title: string;
  emoji: string;
  requires: OrderRequirement[];
  rewards: OrderReward;
  status: 'active' | 'completed';
  timeLimit?: number; // 限时订单截止时间
}

// 建筑
interface Building {
  id: string;           // 'bakery' | 'cafe' | 'flower_shop'
  name: string;
  emoji: string;
  level: number;
  maxLevel: number;
  upgradeCost: number;
  effect: string;       // 升级效果描述
  unlocked: boolean;
}

// 玩家状态
interface PlayerState {
  level: number;
  exp: number;
  expToNext: number;
  coins: number;
  diamonds: number;
  energy: number;
  maxEnergy: number;
  lastEnergyTime: number;
  buildings: Record<string, Building>;
  generators: Record<string, Generator>;
  grid: (GridItem | null)[];
  orders: Order[];
  unlockedChapters: number;
  completedOrders: number;
  totalMerges: number;
}
```

### 5.2 存档格式（JSON）

```json
{
  "version": 1,
  "timestamp": 1780889000000,
  "player": {
    "level": 5,
    "exp": 230,
    "expToNext": 207,
    "coins": 1250,
    "diamonds": 30,
    "energy": 67,
    "maxEnergy": 100,
    "lastEnergyTime": 1780888000000
  },
  "grid": [
    null,
    {"id":"a1","type":"bread","level":2,"emoji":"🥣"},
    null,
    {"id":"a2","type":"coffee","level":1,"emoji":"🫘"},
    ...
  ],
  "generators": {
    "basket": {"id":"basket","level":2,"energyCost":1,"cooldownEnd":0},
    "coffee_pot": {"id":"coffee_pot","level":1,"energyCost":1,"cooldownEnd":0},
    "flower_bush": {"id":"flower_bush","level":1,"lastAutoTime":1780887000000}
  },
  "orders": {
    "main": {"id":"o1","type":"main","title":"制作面包","requires":[...],"status":"active"},
    "side": []
  },
  "buildings": {
    "bakery": {"id":"bakery","level":1,"unlocked":true},
    "cafe": {"id":"cafe","level":0,"unlocked":false}
  },
  "statistics": {
    "totalMerges": 47,
    "completedOrders": 12,
    "totalPlayTime": 3600
  }
}
```

---

## 6. API 设计

### 6.1 游戏操作 API

#### POST /api/v1/game/merge

```json
// 请求
{
  "user_id": "12345",
  "from": {"row": 2, "col": 3},
  "to": {"row": 2, "col": 4}
}

// 响应
{
  "code": 0,
  "data": {
    "result": "merged",
    "new_item": {"type": "bread", "level": 3, "emoji": "🫓"},
    "removed_pos": {"row": 2, "col": 4},
    "chain_merges": [],
    "energy": 98,
    "coins": 0,
    "exp": 5
  }
}
```

#### POST /api/v1/game/produce

```json
// 请求
{
  "user_id": "12345",
  "generator_id": "basket"
}

// 响应
{
  "code": 0,
  "data": {
    "item": {"type": "bread", "level": 1, "emoji": "🌾"},
    "position": {"row": 0, "col": 0},
    "energy": 99
  }
}
```

#### POST /api/v1/game/submit-order

```json
// 请求
{
  "user_id": "12345",
  "order_id": "o1",
  "items": [
    {"row": 3, "col": 5}
  ]
}

// 响应
{
  "code": 0,
  "data": {
    "order_completed": true,
    "rewards": {"coins": 50, "exp": 20},
    "removed_items": [{"row": 3, "col": 5}],
    "new_order": {...}
  }
}
```

---

## 7. 部署方案

### 7.1 P0 部署

```
# 纯静态文件，Nginx 或任何静态服务器
merge2-game/
├── index.html
├── css/
├── js/
└── assets/

# Nginx 配置
server {
    listen 80;
    server_name merge2.example.com;
    root /data/merge2-game;
    index index.html;
}
```

### 7.2 P1+ 部署

```
# Docker Compose
version: '3'
services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./frontend:/usr/share/nginx/html
  api:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - DB_DSN=root:password@tcp(mysql:3306)/merge2
      - REDIS_ADDR=redis:6379
  mysql:
    image: mysql:8.0
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=merge2
  redis:
    image: redis:7-alpine
volumes:
  mysql_data:
```

---

## 附录

### A. P0 快速启动

```bash
# 1. 克隆项目
git clone <repo-url> merge2-game
cd merge2-game

# 2. 直接用浏览器打开
open index.html
# 或启动本地服务器
python3 -m http.server 8080
# 访问 http://localhost:8080

# 3. 开始玩！
```

### B. 技术债务清单（P0 → P1）

| 技术债务 | 说明 | 计划解决 |
|---------|------|---------|
| 纯前端无后端 | 数据只存 localStorage | P1 加后端 |
| 无用户系统 | 单机游玩 | P1 加登录 |
| Emoji 代替美术 | 视觉效果简陋 | P2 换 Unity |
| 无音效 | 静音游戏 | P2 加音效 |
| 无性能优化 | 大棋盘可能卡顿 | P1 优化 |
| 无安全校验 | 客户端可篡改数据 | P1 加服务端校验 |

### C. 物品链扩展计划（P2+）

| 物品链 | 生成器 | 解锁等级 | 最高等级 |
|--------|--------|---------|---------|
| 面包链 🍞 | 菜篮 🧺 | Lv.1 | 10 |
| 咖啡链 ☕ | 咖啡壶 ☕ | Lv.3 | 10 |
| 花链 🌸 | 花丛 🌸 | Lv.5 | 8 |
| 海鲜链 🦐 | 渔网 🎣 | Lv.8 | 12 |
| 珠宝链 💎 | 矿镐 ⛏️ | Lv.12 | 15 |
| 魔法链 🔮 | 魔法书 📖 | Lv.15 | 15 |
